# FastAPI Backend Base URL
VITE_API_BASE_URL=http://localhost:8000

# Optional Mapbox Access Token (if custom vector styles are enabled)
# Defaults to high-resolution OpenStreetMap / CartoDB Dark Matter / GEBCO bathymetric tiles
VITE_MAPBOX_ACCESS_TOKEN=
